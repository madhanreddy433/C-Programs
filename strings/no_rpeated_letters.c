/* TO-Do*/


#include <stdio.h>
#include <string.h>

int main(){

    char str[100];

    return 0;
}




/*
int main(){
    char str[100];
    int i, j, count;

    printf("enter the string:");
    gets(str);

    for(i=0; i<strlen(str); i++){
        count = 0;
        for(j=0; j<strlen(str); j++){
            if(str[i] == str[j]){
                count++;
            }
        }
        printf("%c is repeated %d times in %s\n", str[i], count, str);
    }
    return 0;
}

*/